34401A NVRAM Viewer v0.11 validation

Static checks:
  - Python source compiled successfully with python -m py_compile.
  - No remaining hard-coded BLOCKS map is used by refresh, export, compare, or raw views.
  - Layout-specific CAL string decoder accepts a layout object.
  - B4–B8 PairSpec addresses resolve through the detected correction-word shift.

Automated layout harness:
  - Synthetic confirmed 04/01/01 image: 9/9 legacy blocks pass; 05/02/01+ map does not select it.
  - Synthetic confirmed 05/02/01+ image: 9/9 modern blocks pass; 04/01/01 map does not select it.
  - Legacy CAL string decoded as: HP034401 + 4 NUL + payload starting at packed character 12.
  - Modern CAL string decoded as: HP034401 + 5 NUL + payload starting at packed character 13.
  - DCV 10 V Q23 candidate addresses resolved as W[2E]/W[2F] in legacy and W[54]/W[55] in modern.
  - Fully erased B4 data is marked missing, its Q23 model is disabled, and migration guidance requires re-entry / verification.

GUI smoke test under Xvfb:
  - Constructed the application and loaded a synthetic legacy dump.
  - Verified layout status, decoded primary CAL:STR default, and legacy DCV 10 V physical address display.
  - Loaded a CAL Assistant DCV 10 V block; Console showed Action=Calibrate DCV 10 V and Data=9.999999873, with synchronized CONF/CAL:VAL script.
  - Switched back with Change CAL string; Console restored the CAL:STR write/verify template.

No actual meter commands were sent during validation.
